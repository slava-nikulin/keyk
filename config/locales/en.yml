en:
  application:
    access_denied: Access denied
    invalid_login: "Please, use correct login format: phone or email"
    authorize_error: Authorization failed
    invalid_params: Parameters are invalid
    record_not_found: Record not found
    unauthorized_operation: Operation is unauthorized
    service:
      template:
        invalid_config: Template's config is invalid
  api:
    v1:
      accounts:
        destroyed: Account was destroyed
      templates:
        destroyed: Template was destroyed
      groups:
        destroyed: Group was destroyed

  activerecord:
      errors:
        models:
          user:
            attributes:
              phone:
                wrong_format: Please, use correct phone format
              email:
                wrong_format: Please, use correct email format
          group_relationship:
            attributes:
              group:
                must_have_owner: Group must have at least one owner

          #field:
            #attributes:
              #name:
                #taken:
